# VISION.md — 企業ロゴ自動取得・背景削除ツール

作成日: 2026-08-25 / ステータス: 構想(未着手)

---

## 1. 目的

企業名のリストを渡すと、各企業のロゴ画像をネット上から自動取得し、背景を透過処理し、余白をトリミングした状態で出力する。資料・スライド・一覧表などにロゴを貼る作業を、手作業の「検索 → 保存 → 背景消し → 切り抜き」から解放する。

**最終的な利用先は PowerPoint 資料**である。したがってゴールは「PNGファイルを作ること」ではなく、**PPTにロゴが載った状態**であり、ファイルをダウンロード・保存してから挿入する手間まで含めて省くことを目指す。

最終的には自分以外の人も使える形で配布する。

## 2. スコープ

### やること
- 企業名(日本語/英語)のリストを入力とする一括処理
- 画像検索API経由でのロゴ候補の取得
- remove.bg API による背景透過
- 透過後のアルファチャンネルを基準にした自動トリミング(余白除去)
- PNG(透過)での出力、企業名に紐づくファイル命名
- 失敗した企業の一覧出力と再実行
- **GUI でのロゴ一覧表示と、クリック1回でクリップボードへコピー**(PPTに直接貼り付ける)
- 補助手段としての `.pptx` 出力(全ロゴを1スライドにグリッド配置)

### やらないこと(初期リリース時点)
- ロゴのベクター化(SVG生成)
- ロゴのデザイン加工(色変換、リサイズ以外の編集)
- 企業データベースの構築・保守
- Web サービスとしてのホスティング
- Excel(.xlsx)/CSV での入力(入力は `companies.txt` のみとする)

## 3. 想定利用フロー

```
companies.txt(企業名を1行1社)
        │
        ▼
  [1] 検索クエリ生成      「NTTデータ」→「NTTデータ ロゴ png」
        │
        ▼
  [2] 画像検索API         候補URLを上位N件取得
        │
        ▼
  [3] 候補スコアリング     サイズ・アスペクト比・拡張子・ドメインで並べ替え
        │
        ▼
  [4] ダウンロード         最上位候補を取得(失敗したら次候補)
        │
        ▼
  [5] 背景削除            remove.bg API に投げて透過PNGを得る
        │
        ▼
  [6] トリミング          アルファ値から外接矩形を求めて切り出し + 一定余白
        │
        ▼
  output/NTTデータ.png  +  report.csv(成否・使用元URL)
        │
        ▼
  [7] GUI に一覧表示 → クリックでクリップボードへ → PPT に Ctrl+V
        (補助)全ロゴを1スライドに並べた logos.pptx も出力
```

## 4. 各ステップの設計方針

### [0] 入力
- `companies.txt`(UTF-8、1行1社)のみを入力とする。空行と `#` で始まる行は無視する。
- 1回の実行で扱う件数は**10社以下**を想定。この規模を前提に、並列処理や大量実行向けの最適化は行わず、逐次処理でシンプルに実装する。

### [1] 検索クエリ生成
- 基本形は `<企業名> ロゴ` + 画像形式の指定。英語社名しか通らないケースに備え、クエリのテンプレートは設定ファイルで差し替え可能にする。
- 「株式会社」「(株)」などの法人格表記は検索前に除去するオプションを持つ。

### [2] 画像検索API
- **論点(要調査)**: Microsoft は Bing Search APIs の提供終了を過去にアナウンスしており、現時点で新規利用できるかは未確認。**これは推測を含むため、実装着手前に実際の提供状況を確認する必要がある。**
- 候補: Google Custom Search JSON API(画像検索モード)/ Brave Search API / SerpAPI などの検索代行API。
- 実装上は「検索プロバイダ」を**差し替え可能なインターフェース**として切り出し、どのAPIを使うかを設定ファイルで選べる構造にする。API提供状況の変化にツール全体が引きずられないようにするため。

### [3] 候補スコアリング
検索結果の1位が必ずしも適切なロゴとは限らないため、以下で並べ替える。
- 画像の縦横比(ロゴは横長が多い。極端な正方形・縦長は減点)
- 解像度(小さすぎるものは減点、極端に大きいものも減点)
- ファイル形式(PNG/SVG を優遇、JPEG は減点)
- 取得元ドメイン(企業自身のドメイン、Wikipedia 等は加点)

### [4] ダウンロード
- タイムアウト・リトライあり。取得失敗時は次の候補へフォールバック。
- Content-Type と実際のバイト列の両方で画像かどうかを検証する。

### [5] 背景削除(remove.bg)
- remove.bg API を使用。APIキーは**設定ファイルに置き、コードから読み込む形のみ**とする(キーの値をチャット・ログ・標準出力に出さない)。
- **課金・レート制限**: remove.bg は従量課金で、無料枠には回数と出力解像度の制限がある。想定は1回あたり10社以下なので通常は無料枠に収まる見込みだが(**枠の具体的な条件は Phase 0 で実測して確認する**)、以下は必須仕様とする。
  - 実行前に「これから N 件処理します」と件数を提示して確認する
  - 一度成功した企業は結果をキャッシュし、再実行時にAPIを呼ばない
- 元画像がすでに透過PNGの場合は remove.bg をスキップする(クレジット節約)。

### [6] トリミング
- 透過PNGのアルファチャンネルからアルファ > しきい値 の画素の外接矩形を求め、そこに一定の余白(既定 2%)を足して切り出す。
- 出力サイズの上限(長辺 px)を設定可能にする。

### [7] GUI・クリップボード連携(**本ツールの中核**)
- 処理完了後、取得できたロゴをサムネイルのグリッドで一覧表示する。各サムネイルの下に企業名を出す。
- サムネイルをクリックすると、その透過PNGをクリップボードに載せる。利用者は PowerPoint で Ctrl+V するだけでよい。
- 透過が保たれない候補や取得失敗した企業も一覧に出し、状態(成功/失敗/要確認)が一目で分かるようにする。
- 一覧から個別に「やり直し(次の検索候補で再取得)」ができるようにする。検索1位が不適切だった場合の救済手段。

**実機検証済み(2026-08-25, tools/pptx_paste_matrix.py による自動検証)**
- PowerPoint を COM 自動操作し、形式×貼り付け方法の全組み合わせを機械判定した。
- 結果: **"PNG" 形式(単独または白背景 CF_DIB / CF_HDROP との併載)なら透過・半透明ともフル保持で貼り付く(PASS)**。
- **CF_DIBV5 を載せると PowerPoint がそれを優先し、アルファ破棄で黒塗りになる(FAIL)。絶対に載せない。**
- 採用: `clipboard.copy_for_powerpoint()` = PNG + CF_HDROP(ファイル) + CF_DIB(白背景の保険)。

### 出力
- `output/<企業名>.png`(透過PNG)
- `output/logos.pptx`: 全ロゴを**1スライドにグリッド配置**し、各ロゴの下に企業名を小さく添えたもの。PowerPoint で開いて必要なロゴをコピーすればよく、クリップボード連携が使えない環境でのフォールバックにもなる。
- `report.csv`: 企業名 / 成否 / 使用した画像URL / 取得元ドメイン / エラー内容
- `failed.txt`: 失敗した企業名のみ。これをそのまま再実行の入力に使える。

## 5. 設定とAPIキーの扱い

- `config.toml`(または `.env`)に以下を持つ。**このファイルは配布物・リポジトリに含めない**(`.gitignore` に登録し、`config.example.toml` のみを配布する)。
  - 検索API のプロバイダ名とキー
  - remove.bg のキー
  - 出力先、余白率、上限サイズ、候補件数などのパラメータ
- キーが未設定の場合は、起動時に「どのキーが足りないか」だけを明示して終了する(値は表示しない)。

## 6. エラー処理・再実行

- 1社の失敗で全体を止めない。必ず最後まで走り切り、末尾に成否サマリを出す。
- 中間生成物(検索結果JSON、元画像、透過後画像)を `cache/` に企業名単位で残す。同じ企業を再実行したときは、キャッシュがあれば API を呼ばない。
- `--force` 指定時のみキャッシュを無視して再取得する。

## 7. 配布(**最終確定: 2026-08-25**)

方針は同日中に3転した(GitHub不使用 → OneDrive共有 → **GitHub版**)。決め手は「①みんなが取得も実行でき ②共有ストックが常時見られ ③DLせずクリックコピーで使える」の3条件を、常時稼働PCなしで満たせるのが GitHub 版だけだったこと(検討経緯は[13章](#13-github版みんなが使う本命構成))。

| 経路 | 用途 | 状態 |
|---|---|---|
| **GitHub版(主経路)** | Actions=取得エンジン(Secretsにキー1セット)、リポジトリ=ストック、Pages=検索・コピーUI。全員が使え、誰のPCにも依存しない | 🔶構築中(private段階) |
| exe(`LogoTool.exe` / `LogoToolCLI.exe`) | 個別・オフライン利用向け(各自のAPIキーが必要) | ✅完了(`dist/LogoTool_v0.7.1.zip`) |
| Web共有版(社内LAN) | 補助。所有者が在席中のみの即時利用 | ✅実装済み(任意起動) |
| OneDrive共有 / 単独GitHub | 検討の結果いずれも不採用(前者はDL挿入の手間が残る、後者は配布のみで要件を満たさない) | — |

APIキーの扱い: exe は利用者各自のキー、GitHub版は Secrets に管理者(大島)のキー1セット。いずれもリポジトリ・配布物にキーは含めない。

## 8. ディレクトリ構成(案)

```
背景削除/
├── VISION.md
├── README.md
├── config.example.toml
├── requirements.txt
├── companies.txt              # 入力サンプル
├── src/
│   ├── main.py                # CLI エントリポイント・全体制御
│   ├── config.py              # 設定・APIキーの読み込み
│   ├── search/
│   │   ├── base.py            # 検索プロバイダのインターフェース
│   │   └── google_cse.py      # 実装(他プロバイダは同階層に追加)
│   ├── downloader.py          # 候補スコアリング + ダウンロード
│   ├── bg_remove.py           # remove.bg 呼び出し
│   ├── trim.py                # アルファ基準トリミング
│   ├── report.py              # CSV / failed.txt 出力
│   ├── pptx_export.py         # logos.pptx 生成(グリッド配置)
│   ├── clipboard.py           # 透過PNGのクリップボード転送
│   └── gui/
│       ├── app.py             # ウィンドウ・一覧表示
│       └── thumbnail.py       # サムネイル生成・クリック処理
├── cache/                     # 中間生成物(gitignore)
└── output/                    # 成果物(gitignore)
```

1ファイル1責務、各ファイル 300〜500行以内を目安とする。

## 9. フェーズ計画

- **Phase 0(調査)** ✅完了: ①検索APIは SerpAPI に確定(Bing API は2025年8月廃止、Google CSE は新規受付終了のため)。②remove.bg は size=preview で運用。③クリップボード透過は実機検証で解決([7] 参照)。
- **Phase 1(コア)** ✅完了: 検索 → ダウンロード → remove.bg → トリミング → 出力 をCLIで実装。実データ3社で成功。
- **Phase 2(一括化)** ✅完了: リスト入力・キャッシュ・report.csv / failed.txt・logos.pptx 出力。
- **Phase 3(GUI)** ✅完了: Tkinter で一覧表示・クリックでコピー・個別やり直し(次候補で再取得)。**実用形に到達。**
- **Phase 4(品質)**: 候補スコアリングの調整、実際の企業リストでの精度検証。
- **Phase 5(配布)** ✅完了: README.md 整備、PyInstaller で LogoTool.exe(GUI)+ LogoToolCLI.exe(CLI)をビルドし動作確認済み(`dist/LogoTool_v0.7.1.zip`, 約29MB)。Web共有版(社内LAN)を主配布経路として実装・多角レビューで確定した16件の問題を修正済み。GitHub は不使用と判断([7章](#7-配布確定-2026-08-25)参照)。

### 実装で得た知見(配布時に効く)
- 社内プロキシのTLS検査で certifi では SSL エラーになる。`truststore` で OS 証明書ストアを使い解決(src/net.py)。配布先の社内PCでも同様の効果が見込める。
- SerpAPI はキーをURLクエリで受け取るため、通信例外のメッセージにキーが混入し得る。外に出す文字列は必ず `net.scrub()` でマスクする。

## 12. Web共有版(社内LAN)

### 背景・要件(2026-08-25 決定)
- 配布は exe(各自キー)に加え、**リンク共有だけで使える形**が欲しい。
- 取得したロゴの**履歴(ストック)を全員が見られ、検索して再利用**できること。

### 方式
- 所有者のPCで小さなWebサーバー(Flask)を起動し、`http://<PC名>:8531/` を社内LANで共有する。
- APIキーはサーバー側(config.toml)のみ。閲覧者には渡らない。
- `output/` がそのまま共有ストックになる。トップページに全ロゴを新しい順で一覧表示し、企業名で検索できる。
- 閲覧者ができること: ストック検索/ロゴのダウンロード/右クリックコピーでPPTへ貼り付け/新規取得(所有者のAPI枠を消費)/やり直し/logos.pptx の一括ダウンロード。
- 取得はサーバー内の単一ワーカーで直列処理(API保護)。remove.bg クレジット切れを検知したら新規の背景削除を停止して画面に表示する。
- 残数(SerpAPI/remove.bg)をヘッダーに常時表示する。

### 制約(利用者に周知)
- 所有者のPCが起動している間だけ使える。
- API枠(SerpAPI 250検索/月・remove.bg 無料50回/月)は**リンクを知る全員で共有**。
- 認証は設けない(社内LAN前提)。社内ポリシー上の可否は所有者が確認する。
- LAN経由(非HTTPS)ではブラウザのJSコピーAPIが使えないため、コピーは「画像を右クリック→画像をコピー」を案内する(検証済みのPNG形式が使われ、透過は保持される)。

## 13. GitHub版(みんなが使う本命構成)

### 背景・要件(2026-08-25 決定)
- 「常時稼働PCがない」制約の下で、①みんなが取得を実行できる ②結果が共有ストックに溜まる ③検索できる、を全部満たす。
- 利用者は全員 GitHub アカウントを持っている。
- フォルダ共有案は「DLして挿入」の労力が従来と同じになるため不採用。

### 構成
- **取得** = GitHub Actions(workflow_dispatch)。誰かが「Run workflow」画面で企業名を入力すると、GitHub のサーバー上でパイプラインが動く。APIキーは Secrets に1セット(利用者のキー設定は不要)。
- **ストック** = リポジトリの `docs/logos/` + `docs/index.json`。Action が自動コミット。検索キャッシュは `ci-cache/` に永続化し、再取得のAPI消費を防ぐ。
- **利用** = GitHub Pages(`docs/`)。検索+クリックでコピー(HTTPS なので Clipboard API が全員使える)+ logos.pptx 一括DL。
- 実装: `tools/ci_fetch.py`(CIエントリ)、`.github/workflows/fetch-logos.yml`、`docs/index.html`(静的ビューワー)。

### 段階公開の方針
1. **第1段階(実施中)**: 個人アカウントの private リポジトリに全構築。何も公開されない。コラボレーター(write権限)は取得も可能。
2. **第2段階(判断待ち)**: Pages を ON にして閲覧ページを開放。**個人アカウントでは Pages サイトは実質公開URL**(アクセス制限は GitHub Enterprise Cloud 限定)のため、「ロゴ+企業名リストが社外から見える」ことの社内規定確認を経てから実施する。
3. 将来、会社の GitHub 組織に移管すれば private Pages・組織管理に移行できる(リポジトリ移管で引き継ぎ可能)。

### 留意(個人アカウント運用のリスク、フラットに記録)
- Pages サイトは公開URLになる(noindex は設定済みだが公開は公開)。
- リポジトリ・Secrets が個人資産となり、異動・退職時は移管操作が必要。

## 10. 留意事項(法務・運用)

- 企業ロゴは商標であり、多くの場合著作物でもある。**本ツールは取得を自動化するだけで、利用の可否は利用者の責任**である旨を README に明記する。各社のロゴ使用ガイドラインに従う必要がある。
- 画像検索API経由で得られる画像は第三者が作成・改変したもの(非公式ロゴ、古いロゴ)である可能性がある。**出力は必ず人の目で確認する前提**とし、ツール側でも取得元URLをレポートに残して追跡可能にする。
- 各APIの利用規約(スクレイピング禁止条項、キャッシュ保持期間の制限など)を Phase 0 で確認する。

## 11. 未決定事項

1. 出力ファイル名:企業名そのまま(日本語ファイル名)でよいか、英数字に正規化するか(現状は日本語のまま。今のところ支障なし)
2. Phase 4(候補スコアリングの調整・多様な企業リストでの精度検証)は未着手。運用しながら気になるケースが出た都度、個別に(スコアリング調整 or 「やり直し」機能)対応する方針でよいか

### 確定済み
- 入力形式: `companies.txt`(1行1社)のみ。Excel/CSV 入力は対象外。
- 処理規模: 1回の実行で10社以下。
- 最終利用先: PowerPoint。GUI からクリック1回でクリップボードにコピーする方式を中核とする。
- pptx 出力のレイアウト: 1スライドに全社をグリッド配置(各ロゴの下に企業名)。
- 配布形態: OneDrive/SharePoint 共有を主経路(PC非依存)、exe を併存、Web共有版は補助(任意起動)。GitHub は不使用。
- 検索API: SerpAPI(無料枠 250検索/月)。プロバイダは差し替え可能な構造。
- GUI フレームワーク: Tkinter(標準添付・追加依存なし)。
- クリップボード方式: PNG + CF_HDROP + 白背景CF_DIB。CF_DIBV5 は禁止(実機検証済み)。
